{
  "name": "lead-verification-system",
  "version": "1.0.0",
  "description": "Lead Verification System Frontend",
  "scripts": {
    "build": "tsc && mkdir -p static/js && cp src/*.html static/ && cp dist/*.js static/js/",
    "watch": "tsc -w",
    "vercel-build": "npm install && npm run build"
  },
  "dependencies": {
    "chart.js": "^4.4.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/chart.js": "^2.9.41"
  }
} 